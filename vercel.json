{
  "framework": "vite",
  "buildCommand": "npm install && npm run build --workspace=web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://pidief-app.onrender.com/api/:path*"
    }
  ]
}
